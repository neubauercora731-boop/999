# Expected Report Notes

The report should state that `data.csv` was used as the dataset, list visible fields `name` and `score`, cite the real stdout values, and mention the real command-output screenshot evidence.
