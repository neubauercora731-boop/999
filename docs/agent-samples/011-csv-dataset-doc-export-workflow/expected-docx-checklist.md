# DOCX Checklist

- CSV is not treated as task-book text.
- Report content explains use of `data.csv`.
- Command-output screenshot is real or screenshot missing is explicitly marked.
- No `.docx` template is required for this sample.
- Export mode auto falls back to `generated_report_docx`.
