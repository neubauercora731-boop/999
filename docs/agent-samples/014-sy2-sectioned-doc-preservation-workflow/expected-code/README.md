# Expected Code

This sample focuses on preserving and filling the teacher template, not on pinning one exact source file.

The generated code may be long, but it must come from the website workflow and the run evidence must be real. The final DOCX should place code-related content under `实验代码`; it must not consume, delete, or rewrite original teacher content.
