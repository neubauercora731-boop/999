Use the website full workflow to complete the original `sy2.doc` teacher template.

Requirements:

- Keep every original section, title, instruction, marker, and layout element from the teacher template.
- Do not modify the original course report title, fill instructions, experimental requirements, experimental purpose, experimental content, experimental steps, or section markers.
- Fill code content only under `实验代码`.
- Insert the real command-output screenshot and its Chinese explanation under `实验结果与分析`.
- Fill `问题及思考` with Chinese reflection text.
- Do not add the words `系统填写` anywhere in the delivered DOCX.
- If a real screenshot is unavailable, insert `【截图缺失】` instead of faking evidence.
