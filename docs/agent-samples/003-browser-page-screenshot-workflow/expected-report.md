# Expected Report

The report should state that a real browser rendering screenshot was generated from `index.html`.

If the browser runner fails, the report must state that the webpage screenshot is missing and must not describe an imagined rendered page as real evidence.
