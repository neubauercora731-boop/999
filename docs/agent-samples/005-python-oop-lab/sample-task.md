# Sample Task

Use Python to define a `Student` class with name, student id, and score fields. Implement `display` and `is_pass` methods. Create three objects and print their information. Attach a running result screenshot.
