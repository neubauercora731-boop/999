# DOCX Checklist

- `run-code` screenshot evidence is available for export.
- `generate-screenshot` trace step references the run-code screenshot evidence.
- quality evaluation does not fail with “screenshot not found”.
- DOCX export can insert the real PNG or, if unavailable, explicitly inserts `【截图缺失】`.
- No fake screenshots or fake run results are allowed.
