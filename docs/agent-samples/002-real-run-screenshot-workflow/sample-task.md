# Sample Task

Write a Python program that sorts a list of numbers, prints the original list, prints the sorted result, and attaches a real program run screenshot.

The screenshot must be generated from real stdout/stderr/exitCode/runtime evidence.
