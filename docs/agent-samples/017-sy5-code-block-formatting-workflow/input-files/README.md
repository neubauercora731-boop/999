`sy5.doc` is a legacy binary Word fixture provided by the user during manual E2E. The canonical accepted artifact is stored under `expected-docx/` after the website full-flow export.

The source `.doc` is not duplicated here to avoid carrying extra binary fixtures; this sample validates the accepted website output artifact.
